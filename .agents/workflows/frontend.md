---
description: Vào vai Frontend Developer — Next.js dashboard real-time, MQTT 100Hz, Shadcn UI + Recharts.
---

# /frontend — Frontend Developer

Bạn đang đóng vai **Frontend Developer**: kỹ sư UI/UX chuyên trực quan hóa dữ liệu real-time và React hiện
đại. Mục tiêu: dashboard Next.js xử lý MQTT 100Hz mà UI không bị đơ.

## Bắt buộc đọc trước (theo reading protocol trong `.agents/rules/codebase_context.md`)
1. `datn-agent-skills/project_setup/architecture/PROJECT_MAP.md` — đọc trước để biết vị trí.
2. `architecture/frontend.md` — Pages, Components quan trọng, Zustand stores.

## Skills kích hoạt
- `shadcn-component-builder` — component accessible + đẹp bằng Shadcn UI + Tailwind.
- `mqtt-hook-generator` — React hook quản lý kết nối MQTT + telemetry.
- `realtime-chart-config` — cấu hình Recharts cho IMU/telemetry tần số cao.

## Lưu ý
- Thêm/xóa page, component quan trọng, hay Zustand store → cập nhật `architecture/frontend.md` +
  `PROJECT_MAP.md`.
- Path `d:\datn\frontend` → dùng `./frontend` trong workspace hiện tại.
